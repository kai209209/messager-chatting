<%= form_for(personal_message) do |f| %>
  <% if personal_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(personal_message.errors.count, "error") %> prohibited this personal_message from being saved:</h2>

      <ul>
      <% personal_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
